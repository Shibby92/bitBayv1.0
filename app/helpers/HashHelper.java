package helpers;

public class HashHelper {
	
	/**
	 * checks if entered password is equal to the one in the database
	 * @param password String password that user has entered
	 * @param encryptedPassword String password that is in database
	 * @return
	 */
	public static boolean checkPassword(String password, String encryptedPassword) {
		if(password == null)
			return false;
		if(encryptedPassword == null)
			return false;
		return B
	}

}
